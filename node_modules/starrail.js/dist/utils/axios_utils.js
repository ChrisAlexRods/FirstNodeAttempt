"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.fetchJSON = void 0;
const axios_1 = require("axios");
const axios = new axios_1.Axios({});
/**
 * @param url
 * @param client
 * @param enableTimeout
 */
async function fetchJSON(url, client, enableTimeout = false) {
    const options = { headers: { "User-Agent": client.options.userAgent } };
    if (enableTimeout)
        options.timeout = client.options.timeout;
    const res = await axios.get(url, options);
    try {
        res.data = JSON.parse(res.data);
    }
    catch (e) {
        // do not parse if it is not json due to some error
    }
    return res;
}
exports.fetchJSON = fetchJSON;
